<Window x:Class="PL.Manager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Title="Manager" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Initialization" Click="Buttoninitialization_Click"  Visibility="{Binding InalizationMode}" Grid.Row="0"/>
        <Button Content="Reset" Click="ButtonReset_Click" Grid.Row="1" />
        <Button Content="Engineer's list" Grid.Row="2" Click="EngineersListButton_Click"/>
        <Button Content="Task's list" Grid.Row="3" Click="TasksListButton_Click"/>

    </Grid>
</Window>
